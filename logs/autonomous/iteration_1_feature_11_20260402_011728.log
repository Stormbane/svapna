Feature #11 is done. Here's what was implemented:

**Files created:**
- `src/svapna/dream/__main__.py` — CLI for dream generation (`python -m svapna.dream`), with flags for `--count`, `--min-quality`, `--output-dir`, `--identity-context`, `--skip-scoring`
- `src/svapna/identity/__main__.py` — CLI for identity preamble generation (`python -m svapna.identity`), with flags for `--model-path`, `--lora-path`, `--output-dir`, `--prompt-types`, `--json`
- `tests/test_cli.py` — 12 tests covering all 5 test cases: `--help` for all 3 modules, default config runs, and config overrides via CLI flags

**All 313 tests pass**, including the 12 new ones. Progress is now 11/12 features completed.
