# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
dist/
build/
.eggs/
*.whl

# Virtual environments
.venv/
venv/
env/

# env files (NEVER commit secrets)
.env
.env.local
.env.production
.env*.local

# Scripts that contain secrets (commit the .example template only)
scripts/heartbeat.bat

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Models (too large for git)
models/base/
models/lora/

# Data (raw conversations contain private content)
data/raw/
data/consolidated/
data/dreams/
data/training/
data/journals/
data/metrics/
data/heartbeat/
data/sacred/
data/identity/
data/curated/
data/expressions/

# Vendored / reference firmware binaries (too large, can be re-downloaded)
firmware/xiaozhi-esp-box-3/
firmware/xiaozhi-esp-box-3.zip

# Evaluations are TRACKED (identity evolution record)
# data/evaluations/ is NOT gitignored

# Agent personal files
.ai/soul.md
.ai/memory/
.ai/logs/

# Personal identity + manifesto files (memory tree lives in ~/.narada/, these are local-only)
identity_manifest.json
config/sacred-manifest.yml
manifesto.md
values_reflection.md
svapna_yajna.md

# Compiled training cache (regenerable)
unsloth_compiled_cache/

# MCP config (local claude code integration)
.mcp.json

# Claude Code internal
.claude/

# Test results
test-results/
.pytest_cache/
.coverage
htmlcov/

# Runtime logs
logs/
