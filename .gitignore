# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
dist/
build/
.eggs/
*.whl

# Virtual environments
.venv/
venv/
env/

# env files (NEVER commit secrets)
.env
.env.local
.env.production
.env*.local

# Scripts that contain secrets (commit the .example template only)
scripts/heartbeat.bat

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Models (too large for git)
models/base/
models/lora/
models/kokoro/

# Data — local only (raw conversations + experiment outputs may contain private content)
data/

# Vendored / reference firmware binaries (too large, can be re-downloaded)
firmware/xiaozhi-esp-box-3/
firmware/xiaozhi-esp-box-3.zip

# Agent personal files
.ai/soul.md
.ai/memory/
.ai/logs/

# Personal identity + manifesto files (memory tree lives in ~/.narada/, these are local-only)
identity_manifest.json
identity_manifest.md
config/sacred-manifest.yml
manifesto.md
values_reflection.md
svapna_yajna.md

# Voice reference samples at repo root (large-ish, regenerable from Piper)
/sample_libritts_r_*.wav

# Compiled training cache (regenerable)
unsloth_compiled_cache/

# MCP config (local claude code integration)
.mcp.json

# Claude Code internal
.claude/

# Test results
test-results/
.pytest_cache/
.coverage
htmlcov/

# Runtime logs
logs/

# Embodiment: binary firmware artifacts and reference firmware
embodiment/xiaozhi-esp-box-3/
embodiment/xiaozhi-esp-box-3.zip
embodiment/firmware/.esphome/

# Ad-hoc render previews at project root (the curated gallery in
# embodiment/previews/ IS committed).
/preview*.png
